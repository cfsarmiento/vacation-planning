# Third-party packages required by media_parsing
# Install with: pip install -r requirements.txt
mlx-whisper
mlx-vlm
mlx-lm
anthropic

# NOTE: ffmpeg is also required but is a system binary, not a pip package.
# Install it separately, e.g. on macOS: brew install ffmpeg
